rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Explicitly allow reading templates even if not logged in? 
    // Usually, it's better to require auth for everything in this kind of app.
  }
}
